# DevOS 3.0 CT130 Runbook / Evidence

Date: 2026-07-12  
Host: Thomas / Proxmox `192.168.179.67`  
Node: `pve`  
Container: LXC `130`, hostname `devos3`, IP `192.168.179.130/24`, gateway `192.168.179.254`

## What Changed

- Confirmed old references to `192.168.178.x` were stale for the Proxmox workload network.
- Confirmed VM `130 ollama-zev` existed and was running.
- Created final VM snapshot before decommission:
  - `pre-devos3-decommission-20260712`
- Stopped and destroyed VM `130 ollama-zev` after explicit approval.
- Created LXC `130 devos3`:
  - Debian 12
  - unprivileged
  - nesting enabled
  - `/dev/net/tun` passed through as `dev0`
  - 4 vCPU
  - 8 GB RAM
  - 60 GB disk on `local-lvm`
  - IP `192.168.179.130/24`
- Created post-setup snapshot:
  - `post-devos3-base-20260712`

## Services

Installed and validated:

- Docker Engine + Compose plugin
- PostgreSQL 16 via official `postgres:16` Docker image
- UFW
- fail2ban
- unattended-upgrades
- Tailscale package and daemon
- cron backup job

Docker note:

- Docker CE is pinned at `28.5.2`.
- `containerd.io` is pinned at `1.7.28`.
- Reason: newer `containerd.io`/Docker stack fails inside unprivileged Proxmox LXC with `net.ipv4.ip_unprivileged_port_start` permission errors.
- Maintenance note: these pinned packages are not covered by normal unattended upgrades while held. Periodically check whether the LXC unprivileged `ip_unprivileged_port_start` issue is fixed upstream, then test unpinning in a snapshot before upgrading.

Pin check:

```bash
apt-mark showhold | grep -E 'docker-ce|docker-ce-cli|docker-ce-rootless-extras|containerd.io'
docker version --format 'server={{.Server.Version}}'
containerd --version
```

## Firewall

UFW is active.

Rules:

- SSH `22/tcp`: allowed from `192.168.179.0/24`
- SSH `22/tcp`: allowed from `192.168.178.0/24` because the current admin workstation still routes from `192.168.178.118`
- API `8080/tcp`: allowed from `192.168.179.0/24`
- API `8080/tcp`: allowed from `192.168.178.0/24`

The earlier broad Tailscale UFW rules for `100.64.0.0/10` were removed before `tailscale up`. Do not re-add broad tailnet access until ACLs exist in the Tailscale console.

Postgres is currently bound only to `127.0.0.1:5432` at Docker publish level. After Tailscale ACL/login is complete, expose it deliberately on the Tailscale path or keep access through local maintenance only. If exposed, prefer specific Tailscale member IPs in UFW over the whole `100.64.0.0/10` range.

## PostgreSQL

Container:

- `devos3-postgres`
- image: `postgres:16`
- published: `127.0.0.1:5432->5432/tcp`

Databases:

- `devos_app`
- `devos_audit`

Roles:

- `devos_api`
- `devos_migrator`
- `devos_readonly`

Secrets:

- Stored on CT only: `/opt/devos3/postgres/.env`
- Permission: `600`
- Do not commit this file.

RLS probe:

- Table: `devos_app.public.domain_probe`
- Owner: `devos_migrator`
- `ENABLE ROW LEVEL SECURITY`
- `FORCE ROW LEVEL SECURITY`
- `devos_api` is not table owner.
- Test result: `devos_api` sees `0` rows with no policy, confirming fail-closed posture for the probe table.

Audit append-only probe:

- Table: `devos_audit.public.audit_events`
- `devos_api` has `SELECT, INSERT`
- `devos_api` does not have `UPDATE` or `DELETE`
- Test result: insert allowed, delete denied.

## Backup / Restore

Script:

- `/opt/devos3/scripts/backup-postgres.sh`

Cron:

- `/etc/cron.d/devos3-postgres-backup`
- Daily at `02:17 UTC`

Retention:

- 14 days

Backup location:

- `/opt/devos3/backup`

Remote backup destination:

- Terra host: `192.168.179.128`
- User: `devosbackup`
- Directory: `/srv/backups/devos3-ct130`
- CT130 key: `/root/.ssh/devos3_backup_ed25519`

Restore test:

- Local backup executed.
- `devos_app` dump restored into temporary database `devos_restore_test`.
- Probe table restored with 1 row.
- Temporary restore database dropped after test.
- Remote backup executed to Terra.
- Remote restore tested from `/srv/backups/devos3-ct130/devos_app_20260712T154922Z.dump`.
- Remote restore probe count: `1`.

## Tailscale

Installed and daemon starts after adding `/dev/net/tun` to the LXC config.

Current state:

- `tailscaled` active
- `tailscale0` exists
- node still needs `tailscale up` login/auth key
- UFW does not currently allow the broad tailnet range.

Pending:

- Apply ACL in the Tailscale console before `tailscale up`.
- Authenticate CT130 into the tailnet.
- Apply ACL so Daniel and Andre reach only `5432` and `8080` on CT130.
- Validate from outside LAN that allowed ports work and other ports fail.

## Ollama / Terra

Terra / Proxmox:

- Host: `192.168.179.128`
- Node: `pve`
- Ollama/GPU guest: LXC `100 ct-ai`
- `ct-ai` IP: `192.168.179.231/24`
- `ct-ai` resources: 14 CPU, 24 GB RAM, 200 GB disk on `ssd-ai`
- UI guest: LXC `101 ct-ui`, IP `192.168.179.232/24`

Observed from admin workstation:

- `192.168.179.231:11434` responds and has the large model set:
  - `qwen3:32b`
  - `qwen3-coder:30b`
  - `nomic-embed-text`
  - others

Observed from CT130:

- `192.168.179.231:11434` now responds from CT130 after adding a persistent iptables accept rule in Terra `ct-ai` for source `192.168.179.130/32`.
- `ollama.lan` is mapped locally on CT130 to `192.168.179.231` in `/etc/hosts`.
- `http://ollama.lan:11434/api/tags` works from CT130.
- `/api/tags` latency from CT130, 5 calls: `7.9, 2.5, 2.8, 2.5, 2.5 ms`; average `3.6 ms`.
- Short `qwen3:32b` inference from CT130 returned `ok` in `4411.6 ms`.
- `192.168.179.202:11434` responds, but that is Thomas CT `102 dev-agents`, not Terra:
  - `llama3.2:3b`
  - `qwen2.5:7b-instruct`

Terra firewall change:

- On `ct-ai`, persisted via `netfilter-persistent`:

```bash
iptables -I INPUT 3 -s 192.168.179.130/32 -p tcp --dport 11434 -j ACCEPT
netfilter-persistent save
```

Observation:

- Ollama logs currently report `total_vram="0 B"` for inference compute. That suggests the GPU path should be checked separately, but it was not changed as part of OS-01-B.

Pending / guardrail:

- Do not use `192.168.179.202:11434` as DevOS local inference. It is a Thomas/dev-agents endpoint with small models only.
- No active DevOS consumer was found on CT130 pointing to `192.168.179.202` or old `192.168.178.x` Ollama endpoints.

## Useful Commands

Status:

```bash
ssh codex@192.168.179.130
sudo docker ps
sudo ufw status verbose
systemctl status docker fail2ban unattended-upgrades tailscaled cron
```

Postgres:

```bash
cd /opt/devos3/postgres
sudo docker compose ps
sudo docker exec -it devos3-postgres psql -U postgres
```

Backup:

```bash
sudo /opt/devos3/scripts/backup-postgres.sh
ls -lh /opt/devos3/backup
```

Tailscale login:

```bash
sudo tailscale up --hostname=devos3 --accept-dns=false
```

## Acceptance Status

- Pre-verifications reported: done
- VM 130 final snapshot before decommission: done
- CT130 recreated as Debian 12 unprivileged LXC: done
- Snapshot after setup: done
- Docker installed and container run validated: done
- PostgreSQL 16 up: done
- RLS fail-closed probe: done
- Backup and restore test: done
- UFW audited: done
- fail2ban active: done
- unattended-upgrades active: done
- Tailscale installed and tun-enabled: partial, pending tailnet login and ACL validation
- CT130 to Terra/Ollama large endpoint: done, `.231:11434` reachable from CT130
- `ollama.lan` on CT130 resolves to `.231`: done
- Remote backup outside Thomas: done, Terra destination restore tested
