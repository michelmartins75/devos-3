# DevOS 3.0 Worker (placeholder)

The agent job runner will land in Leva 2 (F4.3). This directory reserves the
package layout for the PULL worker that executes `agent_jobs` with per-job tenant
context via `SET LOCAL app.tenant_id`.
